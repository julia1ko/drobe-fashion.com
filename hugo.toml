# Local dev default; CI overrides via configure-pages (github.io now, custom domain later)
baseURL = "https://julia1ko.github.io/drobe-fashion.com/"
languageCode = "en-us"
title = "DROBE"
theme = []
disableKinds = ["taxonomy", "term"]

[params]
  description = "Your body. Your closet. Your rules. A virtual wardrobe built around a 4D digital avatar of your body."
  tagline = "Your body. Your closet. Your rules."

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true
